#!/bin/bash

DIR=`pwd`
CDIR=`which kolla-build | sed 's?bin/kolla-build?share/kolla/?'`
patchfile=$DIR/kolla.patch

if ! patch -R -p0 -s -f -d $CDIR --dry-run <$patchfile &>/dev/null; then
  patch -p0 -d $CDIR <$patchfile
fi
